import Link from "next/link";import type {GeneratedOutput} from "@/lib/schemas";import {ConfidenceTag} from "@/components/common/Provenance";
export function GeneratedOutputs({slug,outputs}:{slug:string;outputs:GeneratedOutput[]}){return <section className="card module"><span className="module-kicker">Generated work</span><h2 className="module-title">Outputs</h2><div className="output-list">{outputs.map(output=><Link key={output.id} href={`/opportunities/${slug}/outputs/${output.path}`}><span>{output.title}</span><ConfidenceTag value={output.confidence}/></Link>)}</div></section>}
