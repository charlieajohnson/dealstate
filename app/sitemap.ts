import type {MetadataRoute} from "next";import {SITE_URL} from "@/lib/site";export default function sitemap():MetadataRoute.Sitemap{return["","/opportunities","/opportunities/project-nova","/methodology"].map(path=>({url:`${SITE_URL}${path}`,lastModified:new Date("2026-06-21")}))}
