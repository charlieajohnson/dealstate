import Link from "next/link";const sections=[{title:"Derived state",body:"Facts, issues and document status are validated first. Pure functions derive the current snapshot and compare it with prior snapshots. Free-form prose is never the source of truth."},{title:"Visible provenance",body:"A fact needs a source citation. An inference is labelled. Unsupported values remain explicit gaps. Conflicts preserve both values and both sources."},{title:"Weighted scoring",body:"Nine firm dimensions produce a rounded weighted mean. Every dimension runs from 0 to 100 where higher is more favourable. Risk dimensions therefore measure mitigation, not exposure."},{title:"Synthetic limits",body:"Pass 1 has no real ingestion, retrieval or model call. The opportunity manager matches seeded prompts deterministically. Production security, permissions and connectors are outside this demo."}];export default function MethodologyPage(){return <main className="shell methodology page-pad"><span className="module-kicker">Trust model</span><h1>State first. Sources always.</h1><p className="lead">DealState makes uncertainty inspectable rather than smoothing it into prose.</p><div className="method-grid">{sections.map((section,index)=><section className="card module" key={section.title}><span className="numeric">0{index+1}</span><h2>{section.title}</h2><p>{section.body}</p></section>)}</div><section className="method-formula card module"><h2>Source coverage</h2><p><span className="numeric">received required artefacts + sourced key metrics</span> divided by <span className="numeric">all required artefacts + all key metrics</span>. Project Nova computes to <strong className="numeric">46%</strong>.</p></section><p><Link className="btn btn-primary" href="/opportunities/project-nova">Inspect Project Nova</Link></p></main>}
