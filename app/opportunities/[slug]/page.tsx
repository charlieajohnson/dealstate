import Link from "next/link";import {notFound} from "next/navigation";import {CurrentStateCard} from "@/components/opportunity/CurrentStateCard";import {DealScorecard} from "@/components/opportunity/DealScorecard";import {Contacts,DocumentChecklist,LatestChangesPanel,OpenIssuesTable,SuggestedActions,TimelineAudit} from "@/components/opportunity/DashboardPanels";import {OpportunityManagerChat} from "@/components/chat/OpportunityManagerChat";import {GeneratedOutputs} from "@/components/shared/GeneratedOutputs";import {ConfidenceTag} from "@/components/common/Provenance";import {StatusBadge} from "@/components/common/Badges";import {getRepository} from "@/lib/repo";import {computeState,diffState} from "@/lib/state-engine";
export const dynamic="force-static";export async function generateStaticParams(){return(await getRepository().list()).map(({slug})=>({slug}))}
export default async function OpportunityPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const bundle=await getRepository().getBySlug(slug);if(!bundle)notFound();if(bundle.facts.length===0)return <main className="workspace page-pad empty-state"><Link className="quiet-link" href="/opportunities">← Opportunities</Link><h1>{bundle.opportunity.name}</h1><p>{bundle.opportunity.company_name}</p><div className="card module"><h2>State not populated</h2><p>This thin synthetic opportunity exists to make the workspace index credible. Project Nova is the fully populated pass-1 demo.</p><Link className="btn btn-primary" href="/opportunities/project-nova">Open Project Nova</Link></div></main>;const state=computeState({opportunity:bundle.opportunity,facts:bundle.facts,issues:bundle.issues,documents:bundle.documents,scores:Object.values(bundle.scores)});const previous={...state,key_metrics:{...state.key_metrics,arr:{...state.key_metrics.arr!,value:"€17.8m"}}};const changes=diffState(previous,state,bundle.events);return <main className="workspace page-pad"><header className="opportunity-header"><div><Link className="quiet-link" href="/opportunities">Opportunities</Link><h1>{bundle.opportunity.name}</h1><p>{bundle.opportunity.company_name} · {bundle.opportunity.sector}</p></div><div className="header-state"><StatusBadge value={bundle.opportunity.stage}/><StatusBadge value={bundle.opportunity.current_recommendation}/><ConfidenceTag value={bundle.opportunity.confidence}/><span className="muted">Updated <time>{bundle.opportunity.last_updated_at}</time></span><a className="btn" href="#ask">Ask manager</a></div></header><div className="workspace-grid"><div className="left-stack stack"><CurrentStateCard state={state}/><LatestChangesPanel changes={changes}/><OpenIssuesTable issues={bundle.issues}/><DealScorecard weights={bundle.weights} scores={bundle.scores}/><SuggestedActions state={state}/></div><aside className="right-stack stack"><OpportunityManagerChat responses={bundle.chatResponses}/><DocumentChecklist documents={bundle.documents}/><section className="card module"><span className="module-kicker">Missing context</span><h2 className="module-title">Suggested requests</h2><ul className="request-list">{bundle.documents.filter(doc=>doc.status==="missing"||doc.status==="requested").map(doc=><li key={doc.id}><strong>{doc.artefact_type.replaceAll("_"," ")}</strong><span>{doc.notes[0]}</span></li>)}</ul></section><Contacts contacts={bundle.contacts}/></aside></div><div className="bottom-band"><TimelineAudit events={bundle.events}/><GeneratedOutputs slug={slug} outputs={bundle.outputs}/></div></main>}
