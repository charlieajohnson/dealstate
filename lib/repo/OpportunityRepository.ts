import type {ChatResponse,Contact,Document,Event,Fact,GeneratedOutput,Issue,Opportunity,ScoreEntry} from "../schemas";export type OpportunityBundle={opportunity:Opportunity;documents:Document[];facts:Fact[];issues:Issue[];events:Event[];contacts:Contact[];scores:Record<string,ScoreEntry>;weights:Record<string,number>;chatResponses:ChatResponse[];outputs:GeneratedOutput[]};export interface OpportunityRepository{list():Promise<Opportunity[]>;getBySlug(slug:string):Promise<OpportunityBundle|null>}
