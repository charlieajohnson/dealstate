import type {OpportunityRepository} from "./OpportunityRepository";import {SeedRepository} from "./SeedRepository";import {SupabaseRepository} from "./SupabaseRepository";export function getRepository():OpportunityRepository{if(process.env.USE_SUPABASE!=="true")return new SeedRepository();if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY)throw new Error("Supabase mode requires URL and publishable key.");return new SupabaseRepository()}
